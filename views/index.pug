extends layout

block content
  h1= title
  p Welcome to the #[em GO LANDSCAPE]

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The database  has the following record counts:

    ul
      li #[strong Staff:] !{data.staff_count}
      li #[strong Locations:] !{data.locations_count}

